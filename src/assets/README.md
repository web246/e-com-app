Local source for logo and brand assets used in development imports.

- `logo.svg` - source SVG for header and auth screens
- `logo.png` - raster fallback for imports
- `app-icon.png` - high resolution source for icon exports

Note: You may keep source files here and copy optimized icon sizes to `public/assets/` for production.
